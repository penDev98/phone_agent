[build]
  publish = "dist"
  command = "npm run build"

[functions]
  directory = "netlify/functions"

# Redirect /api/* to Netlify Functions (for functions without explicit path config)
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
